package org.example.companies.razorpay;


import java.util.ArrayList;

/**
 * *
 * * @author Pratyay
 *
 *
 * Given a square grid of characters in the range ascii[a-z], rearrange elements of each row alphabetically, ascending.
 * Determine if the columns are also in ascending alphabetical order, top to bottom.
 * Return YES if they are or NO if they are not.
 *
 * Example
 *
 * The grid is illustrated below.
 *
 * a b c
 * a d e
 * e f g
 */
class GridChallenge {

    public static void main(String[] args) {
        ArrayList<String> arrayList = new ArrayList<>();
        arrayList.add("ebc");
        arrayList.add("fgh");
        arrayList.add("cbi");
        gridChallenge(arrayList);
    }

    /**
     *
     * @param grid
     * @return
     */
    private static String gridChallenge(ArrayList<String> grid) {
        return null;
    }
}
